#ReSACO "5G mmWave small-cell MEC" preset: 5x the WLAN radio bandwidth and
#a stronger WAN backhaul, but tiny cells -- devices hand off every few
#seconds (attractiveness dwell times cut to 30/20/10s). Paired with the
#next-gen workloads in simulation.list. Mirrors resaco/scenario.py
#ENV_PROFILES MMWAVE_5G -- if you change one, change the other.
simulation_time=30
warm_up_period=3
vm_load_check_interval=0.1
location_check_interval=0.1
file_log_enabled=true
deep_file_log_enabled=false

min_number_of_mobile_devices=200
max_number_of_mobile_devices=2000
mobile_device_counter_size=200

wan_propagation_delay=0.1
lan_internal_delay=0.005
wlan_bandwidth=1000
wan_bandwidth=50
gsm_bandwidth=0

#all the host on cloud runs on a single datacenter
number_of_host_on_cloud_datacenter=1
number_of_vm_on_cloud_host=4
core_for_cloud_vm=4
mips_for_cloud_vm=100000
ram_for_cloud_vm=32000
storage_for_cloud_vm=1000000

#each mobile device has one host which serves one VM
#all the host runs on a single datacenter due to the out of memory (oom) issue
core_for_mobile_vm=1
mips_for_mobile_vm=4000
ram_for_mobile_vm=2000
storage_for_mobile_vm=32000

#Each policy here is served by ReSACO/bridge/inference_server.py, which must
#be running first: ReSACO/venv/Scripts/python ReSACO/bridge/inference_server.py
#(falls back to a static EDGE_PRIORITY-style heuristic if the bridge or the
#requested algorithm's checkpoint is unavailable).
#RESACO = Reptile-meta-trained SAC (Algorithm 4). The rest are the paper's
#Section V-C baselines, trained by ReSACO/scripts/train_baselines.py:
#SAC_BASELINE (no meta-init), DDPG_BASELINE, A2C_BASELINE, A3C_BASELINE.
orchestrator_policies=RESACO,SAC_BASELINE,DDPG_BASELINE,A2C_BASELINE,A3C_BASELINE

#use ',' for multiple values
simulation_scenarios=THREE_TIER

#mean waiting time in seconds
attractiveness_L1_mean_waiting_time=30
attractiveness_L2_mean_waiting_time=20
attractiveness_L3_mean_waiting_time=10
